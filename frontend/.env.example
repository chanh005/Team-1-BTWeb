# Base URL of the backend API
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Must match backend's REFRESH_COOKIE_NAME so middleware.ts can detect
# session presence (cookie name only - the token value stays httpOnly).
REFRESH_COOKIE_NAME_PUBLIC=refreshToken
